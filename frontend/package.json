{
  "name": "campus-notifications-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_OPTIONS=--openssl-legacy-provider next dev -p 3000",
    "build": "cross-env NODE_OPTIONS=--openssl-legacy-provider next build",
    "start": "cross-env NODE_OPTIONS=--openssl-legacy-provider next start -p 3000"
  },
  "dependencies": {
    "next": "10.2.3",
    "react": "17.0.2",
    "react-dom": "17.0.2"
  },
  "devDependencies": {
    "cross-env": "7.0.3"
  }
}
